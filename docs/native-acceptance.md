# MacPower 0.1.0 原生验收

日期：2026-09-23，Asia/Hong_Kong。交付对象：`build/MacPower.app` 本机开发版。

结论：核心功能已实现，完成本机数据、自动测试、原生页面及真实 CSV 落盘验收。性能与兼容性按以下测量范围记录，尚不满足“所有发布前验收均通过”的条件。网页原型验收独立保留在 `design-qa.md`。

## 环境与构建

- Apple Silicon MacBook Pro，M4 Pro / Mac16,8，macOS 27.0 (26A428)，arm64。
- Swift 6.4 Command Line Tools；代码部署目标 macOS 14，未在 macOS 14/15 或 Intel 实机验证。
- Release 构建成功，应用包具备 Info.plist、原生图标和 LSUIElement 菜单栏配置。本机临时签名通过严格完整性检查。
- 没有安装管理员辅助进程，没有自动注册登录项或请求通知权限。应用保持只读监测。

## 自动测试与实机数据

| 项目 | 结果 | 证据与边界 |
|---|---|---|
| 23 项 Swift Testing 测试 | 通过 | `build/evidence/core-tests-final.log`；包括适配器与输入区分、补码负数、缺失和无效值、拔电旧值、同口径估算与真实来源字段、满电与健康、状态稳定、三态偏好、断点、积分、分钟极值、CSV、保留与提醒规则 |
| 独立系统字段对照 | 17 项通过、0 失败、3 项无法判定 | `build/evidence/live-verification.json`；在诊断读取前后独立读取系统字段，数据在两次读取间变化的样本不计通过。不等于 USB-C 功率表校准 |
| 外接充电 | 实际观察到 | 供电能力 65 W，与变化中的实际输入分别显示；电池保持正号。电量到 100% 而系统仍报告充电时，不提前宣称已充满 |
| 外接电源下的负电池功率 | 实际捕获到 | 诊断记录中外接状态为 true，输入 0 W、电池 −16.905 W、负载 16.905 W；这不是人工拔电测试 |
| 系统满电状态 | 实际观察到 | 21:30 页面显示“外接电源 · 已充满”，电池约 −0.2 W；保留原始微小功率，未伪造零值 |
| 电池供电与估算路径 | 15 项实机对照通过、0 失败 | 后台运行期间于 21:36 左右实际观察到外接状态变为 false，历史新建来源段；21:43 独立对照验证断开输入、电池 V×I 和整机估算，见 `live-battery-verification.json`。未由测试人员控制插拔时点，不代表快速插拔全矩阵 |
| 健康报告 | 实机读取通过 | 系统最大容量 100%、正常、144 次循环；与剩余电量独立。温度和原始容量保留为“未提供 / 未验证” |
| 历史落盘与重启读取 | 通过 | 原子 JSON、正常退出与重开后历史可见；各次退出/间断在曲线中断开。不会补出运行前的数据 |
| 暂停记录与恢复 | 原生操作及落盘验证通过 | 关闭记录后观察 16.51 秒，已有 921 个样本保持不变、历史未删；恢复后落盘样本增至 944，详见 `recording-pause-verification.json` 与 `delivery-manifest.json` |
| 原生 CSV 保存 | 通过 | 通过保存面板导出 `build/evidence/ui-export.csv`；27 条、21 列，均值位于最小/最大之间、来源齐全，详见 `export-verification.json` |

测试中的设备读数只是对应时刻的样本，不是当前固定功率、精度校准或寿命保证。

## 原生交互与视觉

- 已操作实时、趋势、健康与设置页，打开概览窗口；页面宽 416 pt，长内容滚动。独立窗口使用系统标题栏，不冒充菜单栏弹层截图。
- 浅色、深色、跟随系统均可即时选择；已验证设置到其他窗口的颜色同步，手动深色重启保留，最终保存值恢复为 `system`。没有改变用户的系统外观，因此真实系统从浅到深自动切换仍未操作验收。
- 趋势 15 分钟切到 1 小时、开启整机曲线后仍保留输入与电池曲线；完整趋势继承了时段和三条曲线的选择。导出使用当前时段的真实记录。空缺和程序重启处实际断线。
- ⌘W 关闭设置后回到原来的概览页面，⌘Q 正常退出；再次打开应用可恢复可见窗口。关闭全部窗口后采样继续。
- 原生窗口的主读数、正负号、供电能力、来源说明、健康摘要与方案 1 的信息顺序一致。系统 SF Symbols 和原生控件与概念图细节有差异，实际数值来自设备，不使用概念图示例填充。
- 本机截图见 [实时浅色](../design/native/live-light.png)、[实时深色](../design/native/live-dark.png)、[满电/跟随系统](../design/native/live-full-system.png)、[趋势深色](../design/native/trends-dark.png)、[健康深色](../design/native/health-dark.png)、[设置浅色](../design/native/settings-light.png)、[设置深色](../design/native/settings-dark.png)、[设置跟随系统](../design/native/settings-system.png)。
- 最终收尾版本补充 [真实电池供电](../design/native/live-battery-dark.png) 和 [完整趋势](../design/native/history-dark.png)，时间轴边缘标签已完整显示，电池图标反映当前电量。
- [最终浅色/插电放电](../design/native/live-supplement-light.png) 同时验证了琥珀色状态提示、负号和浅色下的电量语义色。测试结束后历史记录为开启，主题恢复跟随系统，通知和登录启动仍关闭。

## 测试发现与修复

1. 主题选择曾触发 `@Published preferences` 的 observer 内自赋值，造成递归崩溃。最小复现和崩溃调用栈定位后，移除 observer 中的回写，仅在读取偏好时验证；原生浅/深/系统连续切换和重开复验通过。
2. SwiftUI 的滚动内容固有高度使独立概览切到趋势时收缩。改为 AppKit 管理尺寸、关闭自动尺寸反推并约束根视图；实时 → 健康 → 趋势切换后窗口保持完整高度。
3. 菜单栏程序缺少标准窗口快捷键。补充“窗口”菜单的关闭/最小化动作，并实测 ⌘W 返回原窗口。
4. 收起后仍保留页面和图表。改为关闭时释放 SwiftUI 承载视图，重新打开时按需创建；主题与记录保留在数据层。
5. 原先 50 MB 的历史读取上限可能拒绝正常 90 天记录，改为匹配 140,000 条记录上限的 256 MB 容量保护。大规模长期历史的性能仍待单独测量。
6. 时间轴末端标签裁切、浅色下电量进度未遵循语义色、满电图标仍用固定 75% 图形，均在最终版本修正并截图复验。电流回退字段的来源名称也改为实际使用的字段，并增加测试。

## 后台与性能

10 分钟测量见 `build/evidence/background-verification.json`，环境及对应二进制摘要见 `acceptance-environment.json`。采样时先实际打开实时、趋势和设置，再关闭所有窗口，期间不打开应用界面。

| 指标 | 实测结果 | 判定 |
|---|---|---|
| 观测时长 | 600.038 秒 | 完成 |
| 平均 CPU（单核 100%） | 0.3566% | 低于 0.5% 目标 |
| RSS 初始 / 结束 / 峰值 | 144.33 / 143.80 / 144.33 MiB | 高于 80 MB 目标，不通过 |
| 后台 physical footprint 两次快照 | 55.9 / 56.0 MiB | 单独报告，不替代 RSS；不是连续峰值 |
| 新增落盘样本 | 122 次，历史文件由 25,183 增至 33,132 字节 | 后台记录与持久化继续运行 |
| 供电来源变化 | 外接电源转为电池供电；来源段变化 | 断开旧来源连续性，无假拼接 |

测量采用累计进程 CPU 时间除以真实经过时间，单核 100% 口径；每 5 秒记录 RSS，同时检查磁盘历史样本增长。RSS 包含驻留的共享框架等页面；另用系统 `vmmap` 记录 physical footprint，二者分别报告。生命周期 footprint 峰值 174.5 MiB 包含界面操作，不能称作后台峰值。测量期间运行过只读内存诊断，未扣除可能产生的诊断开销。

10 分钟测量之后只进行时间轴/进度条/图标、趋势选择继承和来源标签收尾，采样与持久化实现未改变；最终包另做界面复验和短时后台回归，保留两个版本的证据，不将它们混成同一次运行。

最终包后台回归见 `background-final-smoke.json`：120.024 秒内平均 CPU 0.2999%，新增落盘 25 次采样，RSS 保持 154.375 MiB。该次先打开完整趋势等更多页面再收起，内存结果同样未通过 80 MB RSS 目标；无崩溃或持续增长的证据也不等于证明不存在长期泄漏。

最终包后台 physical footprint 快照为 61.3 MiB，含界面操作的生命周期峰值 177.0 MiB，见 `memory-final-bundle.json`。该数值与 RSS 继续并列保留。

原设计“常驻内存 < 80 MB”未明确测量口径，本验收不将较低的 footprint 替代较高的 RSS 以宣称全部通过。短时过程也不代表整日耗电、内存泄漏长期检验或 90 天历史规模性能。

## 仍未完成的发布前验证

| 范围 | 当前边界 |
|---|---|
| 快速反复拔插、低功率供电、优化充电/暂停、睡眠唤醒、多显示器 | 相关逻辑有实现与部分夹具覆盖，已被动观察一次真实电源来源改变，未操作用户硬件或睡眠电脑完成全矩阵 |
| 其他芯片、旧版系统、无内置电池、外设/UPS | 未建立实机兼容矩阵；无服务显示不可用，不补成 0 |
| 独立测量准确性 | 未接 USB-C 功率表；当前只证明与系统字段的转换一致 |
| 通知、登录启动 | 已接系统 API，默认关闭；未授予权限或注册登录项完成端到端验证 |
| 完整无障碍 | 提供语义标签和系统控件；尚未完成 VoiceOver、增强对比度、减少透明度和键盘全流程验收 |
| 大规模与长期稳定性 | 10 分钟后台观察不替代 24 小时运行或 7/30/90 天历史负载测试 |
| 正式发布 | 尚无 Developer ID、Apple 公证、通用二进制、安装器或自动升级链路 |

当前可供本机试用与继续迭代；不把尚未验证的能力标为通过。

交付包与二进制摘要位于 `build/evidence/delivery-manifest.json`。可直接使用 `build/MacPower.app`，或保留 `build/MacPower-0.1.0-arm64.zip` 作为此版本快照。

## 本机安装复验（2026-09-23 22:14，Asia/Shanghai）

- 从工作区 `817d1d1` 构建 0.1.0，安装至 `/Applications/MacPower.app`。本次环境为 macOS 27.0 (26A428)、arm64，选用 `/Applications/Xcode.app/Contents/Developer` 的 Swift 6.3.3；与上文原始验收工具链分别记录。
- 自动测试：重新执行 `zsh scripts/test.sh`，23 项通过。此项为夹具覆盖，不代表新增设备或硬件状态验收。
- 构建与安装：`zsh scripts/build-app.sh` 成功；安装后二进制与构建输出逐字节一致，`codesign --verify --deep --strict` 通过。本次仍为临时签名，不代表 Developer ID 或公证验收。
- 实机与界面：确认进程来自安装目录；实际打开菜单栏弹层及独立概览，再次执行 `open /Applications/MacPower.app` 可显示概览。界面时间更新至 22:14，输入与整机功率从约 16.8 W 变化至约 10.5 W。供电能力、电量和最大容量显示未提供；未据此判定电池字段准确性或完成无内置电池设备兼容验收。
- 持久化：在 `~/Library/Application Support/MacPower/history-v1.json` 观察到文件生成与大小增长；未导出硬件标识符。未操作通知授权、登录启动或充电控制。
- 运行边界：首次 UI 自动化连接超时，后续连接成功；一次直接启动探测进程返回 137，原因未判定。最终安装目录中的应用进程和实时界面正常运行，本次不宣称已通过退出稳定性或长期运行验收。
- 已安装可执行文件 SHA-256：`7b08c358e4d4865a0515c92afa3014460e123a8e3e179b85748c99b04ba23b0b`。

## 菜单栏总功率调整（2026-09-23 22:36，Asia/Shanghai）

- 按用户反馈，将新偏好的菜单栏默认指标由电池净功率改为整机耗电（`Preferences.menuMetric = .system`）。已有手动选择仍按保存值读取；本机通过原生设置显式选中“整机耗电”，重开后确认保留。README 与当前设计规则同步更新。
- 自动验证：`zsh scripts/test.sh` 的 23 项现有测试通过，`zsh scripts/build-app.sh` 成功；未将夹具结果计为新硬件验收。构建二进制 SHA-256：`34db7d0a4a74f304ed0c06f64574c157c4867265f76870386f05e4a22dbac95b`。
- 实机验证：新版从 `build/MacPower.app` 启动后，设置页显示“整机耗电”，实时页的整机读数持续更新（22:36 约 4.9 W），电池净功率仍独立显示 0.0 W。偏好落盘值为 `system`。本次未直接捕获菜单栏数字截图，也未重新验证功率准确性、充放电状态矩阵或长期运行。
- 安装验证未通过：新版在 `/Applications/MacPower.app` 及 `~/Applications/MacPower.app` 启动后被终止；严格签名完整性校验通过，但日志同时出现临时签名校验信息，不能单凭该日志判定全部启动失败的根因。改用完整包移动及重新签名也未恢复这些路径的启动，未调整系统安全策略。
- 最终状态：当前运行的是项目内的 `build/MacPower.app`；`/Applications/MacPower.app` 恢复为前次安装包，保存的整机耗电选择保留。安装目录冷启动仍需单独解决，不能称新版安装验收通过。

## v0.1.1 发布复验（2026-09-23，Asia/Shanghai）

- 版本为 0.1.1、构建号 2；“关于”页改为读取应用包版本。菜单栏默认整机耗电，现有已保存选择保留，本机保存值为 `system`。
- 自动测试：本轮 `zsh scripts/test.sh` 的 23 项现有测试通过。`zsh scripts/package-release.sh` 调用 `zsh scripts/build-app.sh` 构建成功，严格签名完整性校验及 ZIP 校验和检查通过。打包已排除构建机器的扩展属性和 AppleDouble 元数据。
- 安装文件：从本次发布 ZIP 解压安装到 `/Applications/MacPower.app`，确认 Info.plist 为 0.1.1 / 2，安装二进制与发布包一致。二进制 SHA-256 为 `ed2d1b2c2532229209fa344b85b0e39b4bf3a8a9c32d5f82fa203db8797bf739`。已有历史与偏好保留。
- 启动边界：安装后进程未保持运行。22:46 的系统日志记录该安装路径的 Gatekeeper 评估错误 `-67018`（未匹配允许策略）及 `was allowed: 0`；分发检查也报告临时签名和缺少公证票据。源码测试通过、签名完整性通过不代表获得 Gatekeeper 放行。
- 本轮未修改全局安全设置，单应用放行等待用户明确确认；本机没有可用 Developer ID 签名身份。发布维持预览版，并在发行说明中公开启动验收未完成的边界，不宣称正式签名、公证或完整安装运行验收通过。
- 与自动测试分别记录：本轮尚未验证 0.1.1 安装后的原生页面、菜单栏截图、长时间采样及冷启动；上一节 0.1.0 构建的实时页面结果不计作 0.1.1 安装验收。发布附件 `verification.json` 记录提交、构建及文件校验结果，不附带个人历史或原始设备诊断。

## 源码拉取与应用程序安装复验（2026-09-23 22:59，Asia/Hong_Kong）

- 源码从 `817d1d1` 快进至 `origin/main` 的 `f79d4d1`（v0.1.1）；本次环境为 macOS 27.0 (26A428)、arm64、Command Line Tools Swift 6.4。
- 自动测试：重新执行 `zsh scripts/test.sh`，23 项通过；此项仅为夹具覆盖。日志为 `build/evidence/install-v0.1.1-tests.log`。
- 构建与安装：执行 `zsh scripts/build-app.sh`，单独输出至 `build/install/MacPower.app`，随后安装至 `/Applications/MacPower.app`。版本为 0.1.1、构建号 2；安装包严格签名完整性检查通过，可执行文件与构建输出逐字节一致。构建日志为 `build/evidence/install-v0.1.1-build.log`。
- 实机与界面：正常退出项目目录旧进程后，从安装路径启动成功；确认运行进程为 `/Applications/MacPower.app/Contents/MacOS/MacPower`，实际打开菜单栏弹层及独立概览，实时供电状态继续刷新。本次结果独立于前文安装失败记录，不归因或覆盖此前失败。
- 本次未修改已有历史、偏好、通知授权、登录启动或系统安全设置；只验证当前设备的安装与短时启动，不代表功率校准、其他设备兼容性、长期运行、Developer ID 签名或 Apple 公证验收。
- 已安装可执行文件 SHA-256：`847189248168175ba39d97aaa9d19270ad255b32210769e466937564be7f9d06`。

## 紧凑菜单栏与能源模式（2026-09-23 23:11，Asia/Hong_Kong）

- 菜单栏改为内容自适应宽度，图标 18 × 10 pt、数字 11 pt，移除前导空格及 W 前空格；保留用户当前选择的指标和电量显示偏好。放电时数字按自动蓝色、节能绿色、高性能橙色区分，图标仍为单色模板，悬停和无障碍描述包含模式与来源。
- 模式来自只读系统配置，面板新增文字和符号。按当前供电来源选择配置，插电补充放电使用外接电源模式；缺失、未知及过期结果不推定为自动。
- 自动验证：`zsh scripts/test.sh` 的 29 项测试通过，其中新增 6 项覆盖三种模式、两种供电来源、旧格式、缺失/异常/冲突值、插电放电以及不可用状态。`zsh scripts/build-app.sh` 构建和严格签名完整性校验通过。日志见 `build/evidence/compact-energy-tests.log`、`compact-energy-build.log`。
- 实机读取：macOS 27.0 / arm64，实际 `pmset -g custom` 报告电池 powermode=1、外接 powermode=2；诊断工具经真实读取链路分别解析为 lowPower / highPower，当前 externalConnected=false。
- 实机界面：已正常退出前版并安装至 `/Applications/MacPower.app`，确认运行进程来自该路径。实际窗口和截图显示“电池供电”“能源模式 · 节能”，电池约 −17.0 W、97% 为当时读数；新增模式条、功率区和健康区未见重叠。该截图只覆盖当前深色页面和节能状态，没有取得独立菜单栏截图或实测菜单栏像素宽度；自动/高性能及旧版系统仅有解析夹具覆盖，未切换用户系统能源模式完成实机矩阵。
- 已保留前版应用至 `build/backups/MacPower-before-compact-20260923-231023.app`，沿用历史、主题、指标和登录启动设置；本轮未注册登录项、申请通知权限或修改系统安全设置。当前包仍是本机临时签名，短时启动不代表长期性能或公证验收。
- 已安装可执行文件 SHA-256：`628db9db560d6fc137b57a0ded36ae1c8531bb669c603295c4d5ee56d9ca8369`。

## 版本更新功能（2026-09-24，Asia/Shanghai）

- 在 `b0bcb81` 基础上增加版本检查与浏览器下载入口。源码基线已执行 `git fetch origin` 并确认与 `origin/main` 一致；本节对应本地未发布的功能改动，GitHub v0.1.1 附件仍为旧版。应用包版本沿用 0.1.1 / 2，未创建新发行版。
- **自动验证**：`zsh scripts/test.sh` 共 46 项通过，其中 17 项新增测试覆盖 SemVer 数值及预发布顺序、预览渠道、草稿过滤、乱序发布、架构附件、可信下载路径、空/异常响应、网络及限流错误、重复点击、取消过期请求、每日调度及重启节流、开发版和降级保护。HTTP 成功与新版本场景使用模拟响应，不能视为真实升级安装验收。日志为 `build/evidence/version-updates-tests.log`。
- **构建**：`zsh scripts/build-app.sh` 成功，临时签名完整性校验通过，输出 `build/MacPower.app`；日志为 `build/evidence/version-updates-build.log`。未更新 `/Applications/MacPower.app`，未发布到 GitHub。
- **实机与原生界面**：macOS 27.0 (26A428)、arm64，工具链 Swift 6.3.3。正常退出已安装旧版后从构建目录启动，实际打开“设置 → 软件更新”，确认包版本显示、自动检查默认关闭、预览版默认开启；检查前及错误态页面在本机跟随系统的深色外观中完整可读。应用菜单“检查更新…”能定位该页面。
- **真实网络**：本机匿名 GitHub 请求返回 403，响应报告匿名配额用尽；原生页面正确显示限流和 14:59:27 后可重试的提示。再次通过菜单检查沿用等待时间；“所有发布版本”在 Chrome 打开 `https://github.com/Linjay/MacPower/releases`，页面标题为 `Releases · Linjay/MacPower`。这证明失败和备用入口可用，不代表本次真实网络已成功获取版本列表。另用已登录 CLI 只读确认现有 v0.1.0 / v0.1.1 均为预览版；应用自身不使用 CLI 凭证。
- **边界**：新版本说明和下载选择、正式版筛选、自动检查节流由夹具覆盖；本轮未执行实际新版本下载/替换、24 小时自动检查、Intel/旧系统兼容、浅色及完整无障碍矩阵。未触发通知授权、登录启动、系统安全变更或充电控制；功率准确性与硬件状态验收沿用前文，未作新增结论。

## v0.1.2 发布构建（2026-09-24，Asia/Shanghai）

- 发布版本更新至 0.1.2，构建号 3；相较 v0.1.1，包含版本检查及 `b0bcb81` 的紧凑菜单栏、能源模式功能。保持预览版分发，未改变 Developer ID 签名和公证边界。
- **自动验证**：重新执行 `zsh scripts/test.sh`，46 项测试全部通过；`zsh scripts/package-release.sh` 调用 `zsh scripts/build-app.sh` 成功，严格签名完整性校验通过。对应日志为 `build/evidence/release-v0.1.2-tests.log` 和 `release-v0.1.2-build.log`。
- **安装包验证**：`build/releases/MacPower-0.1.2-arm64.zip` 共 9 个 ZIP 条目，CRC 检查通过；其中所有文件与构建包逐字节一致。确认 Info.plist 为 0.1.2 / 3，未包含 AppleDouble、扩展属性、个人历史或诊断数据。ZIP SHA-256 为 `1c47427215e14b9644221cf90c21d4ced3e7601cd8fb144a777f6c3cd9e3024d`。
- **实机证据边界**：上一节原生 UI 验证使用功能相同、包版本仍为 0.1.1 的本地构建；本节验证版本元数据与分发包，未将其计作 0.1.2 下载后安装启动验收。未覆盖其他设备、长期运行、真实下载替换或新权限流程。
- 发行说明位于 `docs/releases/v0.1.2.md`；随包 `verification.json` 记录实际源码提交及验证摘要，`SHA256SUMS` 用于核对 ZIP。
