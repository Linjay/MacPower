# MacPower 方案 1 · 设计与交互验收

日期：2026-09-23。范围：可交互网页设计原型。final result: passed

本结论针对选定方案的视觉与原型交互，不代表原生程序、传感器准确性、真实系统通知或硬件兼容性通过验收。

原生程序现已实现，独立验收与真实界面证据见 [原生验收记录](docs/native-acceptance.md)。本文件保留网页原型当时的测试结果，不把它改写为原生通过记录。

## 对照基准与证据

- 浅色视觉目标：[design/concept-01.png](design/concept-01.png)。深色目标：[design/concept-01-dark.png](design/concept-01-dark.png)。均为 1058 × 1487 像素。
- 实际实现：http://127.0.0.1:4173/，React 网页原型；截图为浏览器真实渲染，不使用参考图充当界面。
- 正常浏览器视口 724 × 863 CSS px，报告 DPR=2，但浏览器截图输出为 724 × 863 实际像素。因此按截图宽度 / CSS 视口宽度归一，不再次乘 DPR。
- 对参考图裁剪面板区域 `(151,71)-(909,1433)`，758 × 1362 px，等比缩至 416 × 747；实现按 DOM 边界裁剪为约 416 × 759 px，保留自身高度、不强行拉伸。
- 状态一致：中文、实时页、充电、61.6 W 输入、34.0 W 整机、+27.6 W 电池、65% 电量、65 W 供电能力、100% 最大容量、144 次循环、19:42 示例时间。
- 全屏并排对照：[浅色](design/qa/comparison-light.png)、[深色](design/qa/comparison-dark.png)。每个文件同时包含参考和实现，左参考、右实现。
- 局部放大：[功率区域浅色](design/qa/focus-light.png)、[功率区域深色](design/qa/focus-dark.png)。检查了标签、数字、单位、图标、分支箭头和供电能力说明。
- 原始完整截图和对应 DOM 边界 JSON 保存在 `design/qa/*-full.png`、`*-bounds.json`，可追溯裁剪过程。

## 发现、修正与复验

1. **P1，已修正：功率流向图背景黑块、节点空间不足。** 初次渲染使用了图库默认表面色且高度不足。明确透明背景，以容器实测宽度缩放，并重排节点；复验完整显示三个读数。证据：最终深浅色全屏对照。
2. **P2，已修正：辅助文字过小、主体区域偏松。** 提升指标标签、供电能力、状态、健康摘要及设置文字，收紧功率区域多余高度。最终主面板约 759 px 高，参考约 747 px；额外高度为可接受的字体/真实图表适配差异，阅读顺序和主要分区保留。
3. **P2，已修正：分支斜线弱化了方案 1 的流向结构。** 使用图库带圆角的水平分支，保留由电源分向整机和电池的箭头。电池供电仍以电池指向整机。证据：最终局部对照。
4. **P1，已修正：图中说明按钮无法点击。** 静态图库节点继承了禁用指针事件；为指标与供电能力按钮恢复点击。实测可打开 61.6 W 输入说明，文案区分直流输入、供电能力与墙上交流耗电。
5. **P2，已修正：关闭弹窗后键盘焦点丢失。** 记录打开弹窗的按钮，关闭后恢复焦点。实测 Escape 关闭输入说明、焦点返回“查看电源输入说明”；背景按钮在弹窗打开时隐藏。
6. **P2，已修正：手动主题影响模拟系统背景。** 将系统外观和应用有效主题分开；手动深色配系统浅色时，只有应用内容采用深色。复验通过。
7. **P2，已修正：鼠标点击主题卡产生额外焦点框。** 改为仅键盘可见焦点，选中状态由描边和勾选表达。[最终设置对照](design/qa/appearance-comparison.png)。

经过修正、重新截图与并排复核，未发现仍需处理的 P0/P1/P2 设计问题。

## 五项视觉检查

| 项目 | 检查结果 |
|---|---|
| 字体与排版 | 使用 macOS 系统字体与中文回退；功率数字最大、指标其次、来源最弱。数字使用等宽数字特性。局部检查没有截断或单位错位。生成图无法识别唯一字体，系统字体为实施选择；浏览器抗锯齿与生成图存在可接受差异。 |
| 间距与布局 | 保留标题、分段标签、电力分流、电量、短趋势、健康的顺序和整体比例。主面板宽 416 px；设置页 660 px。长趋势和健康页面在固定面板内滚动；正常页面与 390 px 窄视口均无横向溢出。 |
| 颜色与状态 | 浅深色均使用语义 token，输入蓝色、充电青色、持续插电放电琥珀色；同义信息不因主题换色。箭头、正负号和文字提供额外区分。参考的磨砂光照简化为可读实色表面，是网页原型的预期差异。 |
| 图片与图标 | 使用实际图标库、React Flow 与 Chart.js，不将整张生成图片作为界面。标准插头、电脑、电池、齿轮和心形保持同类语义；细节不同于生成图，接受为库图标差异。应用闪电标记采用标准图标，正式原生品牌资产待开发阶段导出。保留 React Flow 署名，未隐藏。 |
| 文案与内容 | 核心数值、供电能力解释、充电方向、健康分母、示例标记保留；未提供项显示“未提供”或“—”。缺失输入和整机功率不伪造为零。历史无记录时不生成健康曲线。 |

## 交互验证

- 三态主题：默认跟随系统；手动浅/深色不被模拟系统改变覆盖；切回系统即刻同步；刷新保留偏好。真实浏览器当前有效外观为深色，初始读取正确。
- 主题切换保留趋势页、时段和曲线选择；设置页即时保存、不要求另点保存。
- 菜单栏指标切为电源输入后显示 61.6 W；额外电量开关可操作，测试后恢复默认电池净功率。
- 15 分钟、1 小时、24 小时切换，三条曲线可显隐并保留至少一条；键盘左右键可查看取样点，完整趋势继承选择。
- 七种场景验证含充电、电池供电、暂停、插电仍在放电、部分缺失、过期、满电。读数与方向结果保存在 [scenario-checks.json](design/qa/scenario-checks.json)。
- 关闭示例历史后出现无记录状态，恢复后重新显示示例图表。
- 数值逻辑检查通过：主题解析、无效偏好回退、正负号/空值格式化、CSV 缺失列为空而非零。
- 窄窗口 390 × 844：面板宽 366 px，设置窗宽 358 px，文档滚动宽度 390 px，无横向溢出；测试后已恢复正常视口。[面板](design/qa/narrow-live.png)、[设置](design/qa/narrow-settings.png)。
- [趋势浅色](design/qa/trends-light.png)、[趋势深色](design/qa/trends-dark.png)、[健康浅色](design/qa/health-light.png)、[健康深色](design/qa/health-dark.png)、[完整趋势](design/qa/history-light.png) 已作渲染检查。长内容使用内部滚动。
- 最终 `npm run build` 成功。首次开发时的图库布局警告已修复；最终重新加载后没有观察到新增 error/warn。开发热更新曾产生 nodeTypes 提示，源码采用模块级稳定定义。

## 残余测试空白与 P3 后续事项

- 没有改变用户真实 macOS 外观；系统自动切换用演示控制模拟，真实 `matchMedia` 监听已接入但跨时段系统切换未实测。
- 未进行 VoiceOver、增加对比度、真实多显示器或原生菜单栏键盘验收；网页键盘流程不替代这些测试。
- CSV 内容生成与缺失语义已验证；点击导出后当前内置浏览器未返回下载事件，实际下载文件落盘尚未验证。界面仅提示已生成 CSV，不宣称已保存到磁盘。
- 构建存在单包超过 500 kB 的体积提示，原型可使用；正式应用应替换为原生绘制并单独测量运行开销。
- 原生数据、权限、历史存储、通知、启动项、睡眠唤醒、健康解释准确性与硬件兼容性仍按产品文档进行后续验收。

## 交付检查

- [x] 选定视觉方向及深色扩展均有可打开参考。
- [x] 深浅色实际页面与参考已归一并排对照，并检查局部。
- [x] 修复发现的 P0/P1/P2，重新截图复验。
- [x] 三态外观规则、保存与页面一致性通过原型验证。
- [x] 本地服务保持运行，预览停留在外观设置，默认“跟随系统”。
- [x] 产品、交互、外观与 README 状态同步；明确示例/原生交付边界。
